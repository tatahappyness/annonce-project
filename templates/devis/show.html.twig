{% extends 'base.html.twig' %}

{% block title %}Devis{% endblock %}

{% block body %}
    <h1>Devis</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ devi.id }}</td>
            </tr>
            <tr>
                <th>DetailProject</th>
                <td>{{ devi.detailProject }}</td>
            </tr>
            <tr>
                <th>FirstName</th>
                <td>{{ devi.firstName }}</td>
            </tr>
            <tr>
                <th>UserName</th>
                <td>{{ devi.userName }}</td>
            </tr>
            <tr>
                <th>PhoneNumber</th>
                <td>{{ devi.phoneNumber }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ devi.email }}</td>
            </tr>
            <tr>
                <th>ZipCode</th>
                <td>{{ devi.zipCode }}</td>
            </tr>
            <tr>
                <th>IsAcceptedCondition</th>
                <td>{{ devi.isAcceptedCondition ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Civility</th>
                <td>{{ devi.civility }}</td>
            </tr>
            <tr>
                <th>IsAskDemande</th>
                <td>{{ devi.isAskDemande ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>DateCrea</th>
                <td>{{ devi.dateCrea ? devi.dateCrea|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>DevisIsAccepted</th>
                <td>{{ devi.devisIsAccepted ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>DevisIsValidated</th>
                <td>{{ devi.devisIsValidated ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>DevisIsFinished</th>
                <td>{{ devi.devisIsFinished ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('devis_index') }}">back to list</a>

    <a href="{{ path('devis_edit', {'id': devi.id}) }}">edit</a>

    {{ include('devis/_delete_form.html.twig') }}
{% endblock %}
