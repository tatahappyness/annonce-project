<form method="post" action="{{ path('mode_prix_delete', {'id': mode_prix.id}) }}" >
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ mode_prix.id) }}">
    <button class=" btn btn-danger"><i class="fa fa-times"></i> </button>
</form>
