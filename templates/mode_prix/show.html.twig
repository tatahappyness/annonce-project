{% extends 'base.html.twig' %}

{% block title %}ModePrix{% endblock %}

{% block body %}
    <h1>ModePrix</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ mode_prix.id }}</td>
            </tr>
            <tr>
                <th>PrixTitle</th>
                <td>{{ mode_prix.prixTitle }}</td>
            </tr>
            <tr>
                <th>PrixDescription</th>
                <td>{{ mode_prix.prixDescription }}</td>
            </tr>
            <tr>
                <th>PrixImage</th>
                <td>{{ mode_prix.prixImage }}</td>
            </tr>
            <tr>
                <th>PrixGlobale</th>
                <td>{{ mode_prix.prixGlobale }}</td>
            </tr>
            <tr>
                <th>PrixMoyen</th>
                <td>{{ mode_prix.prixMoyen }}</td>
            </tr>
            <tr>
                <th>PrixHautGamme</th>
                <td>{{ mode_prix.prixHautGamme }}</td>
            </tr>
            <tr>
                <th>PrixDateCrea</th>
                <td>{{ mode_prix.prixDateCrea ? mode_prix.prixDateCrea|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>
     <div class="modal-footer">
                            <button type="button" class="btn btn-secondary"
                            data-dismiss="modal">
                                NON
                            </button>
                            
                            {{ include('mode_prix/_delete_form.html.twig') }}
                                                                                      
                        </div>

    <a href="{{ path('mode_prix_index') }}">back to list</a>

    <a href="{{ path('mode_prix_edit', {'id': mode_prix.id}) }}">edit</a>

    
{% endblock %}
