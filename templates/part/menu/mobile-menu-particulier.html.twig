<!-- bEGIN MENU DISPALY WHEN MOBILE & TABLETE -->
<a class="text-decoration-none logo-content float-left d-block d-lg-none" href="{{ path('home_page') }}">
<img class="logo" src="{{ absolute_url('/uploads/logo/' ~ configsite.getImage()) }}"></img>
</a>
<ul class="navbar-nav d-none d-lg-none mt-2 mt-lg-0 d-sm-flex flex-sm-column flex-md-row">
    <li class="nav-item active m-1">
    <a class="nav-link" href="javascript:void();"><span class="d-flex align-content-baseline"><span class="lnr lnr-phone-handset text-wite bg-warning p-1"></span> <span class="bg-darkgray p-1 text-nowrap" style="font-size: 15px;">{{ configsite.getNumphone() }}</span></span></a>
    </li>
    <li class="nav-item active m-1">
    <a class="nav-link" href="javascript:void()"><span class="d-flex align-content-baseline"><span class="lnr lnr-envelope text-wite bg-warning p-1"></span> <span class="bg-darkgray p-1 text-nowrap text-lowercase" style="font-size: 15px;">{{ configsite.getEmail() }}</span></span></a>
    </li>
</ul>
<a href="{{ path('space_pro_page') }}" class="btn btn-round bg-white d-none border-warning font-weight-bold text-success text-nowrap text-decoration-none">
    <i class="">👷🏾</i> <label class="sr-only-focusable"> Vous êtes un pro ?</label>
</a>
<a class="openbtn d-block d-lg-none text-center bg-transparent float-right" onclick="openNav()">
    <span class="lnr lnr-menu-circle text-warning" style="font-size: 64px !important;"></span>
</a>

<div id="mySidepanel" class="sidepanel d-block d-lg-none">
    <a href="javascript:void(0)" class="closebtn" onclick="closeNav()">&times;</a>
			  
    <a class="collapsible">Demander un devis</a>
    <div class="content-mobile" style="height: 335px; overflow: scroll;">
        <div class="list-group">
        {% if categories != null %}
            {% for categ in categories %}
            <a href="{{ path('post_ask_devis_page', {id: categ.getId()}) }}" class="list-group-item list-item-mobile">{{ categ.getCategTitle() }}</a>
            {% endfor %}
        {% endif %}
        <a href="{{ path('view_all_tavaux_page') }}" class="btn text-warning pt-2 list-item-mobile">Voir tous les artiles >></a>
        </div>
    </div>
    <a class="collapsible">Guides des prix</a>
    <div class="content-mobile" style="height: 335px !important; overflow: scroll;">
        <div class="list-group">
        {% if categories != null %}
        {% for categ in categories %}
            <a href="{{ path('category_sous_category_page', {categId: categ.getId()}) }}" class="list-group-item list-item-mobile">
                {{ categ.getCategTitle() }}
            </a>
        {% endfor %}
        {% endif %}
        </div>
    </div>
    <a href="{{ path('how_to_step_page') }}" class="collapsible">Comment ça marche ?</a>
    <a href="{{ path('space_find_pro_page') }}" class="collapsible">Trouvez un pros</a>
    <a href="{{ path('logout') }}" class="text-decoration-none collapsible text-warning font-weight-bold text-nowrap">
        <i class="">👷‍♂️‍</i>  Se déconecter
    </a>

			  
</div>
<!-- END MENU DISPALY WHEN MOBILE & TABLETE -->