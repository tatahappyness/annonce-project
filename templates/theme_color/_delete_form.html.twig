<form method="post" action="{{ path('theme_color_delete', {'id': theme_color.id}) }}" >
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ theme_color.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
