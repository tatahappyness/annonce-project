{% extends 'base.html.twig' %}


{% block body %}
    <!--  table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ type.title }}</td>
            </tr>
            <tr>
                <th>DateCrea</th>
                <td>{{ type.dateCrea ? type.dateCrea|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table -->


        <div class="modal-body">
            <span class="font-weight-bold " >Type:</span>
            {{ type.title }}</br>
            <span class="font-weight-bold " >Date de création:</span>
            {{ type.dateCrea() | date('d/m/y H:m:s') }}
        </div>
        <div class="modal-footer">
            <button type="button" class="btn btn-secondary" data-dismiss="modal">
                NON
            </button>

            <form method="post" action="{{ path('type_delete', {'id': type.id}) }}" >
                <input class="btn btn-success" type="hidden" name="_method" value="DELETE">
                <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type.id) }}">            
                <button class="btn btn-success">Delete</button>
            </form>

        </div>
        
    <a href="{{ path('type_index') }}">back to list</a>

    <a href="{{ path('type_edit', {'id': type.id}) }}">edit</a>
    
    {{ include('type/_delete_form.html.twig') }}
{% endblock %}
